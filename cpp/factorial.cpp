#include <iostream>
using namespace std;
int main()
{
    char word[] = "the first word";
    for (size_t i = 'a'; i <= 'z'; i++)
    {
       if (word[i] <= word[i+1]{

       } )
    }
cout<<endl;
    return 0;
}
#include<bits/stdc++.h>
        using namespace std;


        int main()
        {
            ios::sync_with_stdio(false);
            string str = "sharlock";

            sort(str.begin(), str.end());
            cout<<str<<endl;

            return 0;
        }
